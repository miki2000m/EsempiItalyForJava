// NOTA: Questo file è stato rinominato in .txt per evitare errori di compilazione
// in un progetto a modulo singolo. In un vero progetto multi-modulo, si chiamerebbe
// "module-info.java".

module app.main {
    // Questa direttiva dichiara che il modulo 'app.main' ha bisogno
    // del modulo 'app.service' per funzionare.
    // Senza questa riga, il codice in 'app.main' non potrebbe accedere
    // alle classi del modulo 'app.service'.
    requires app.service;
}
