// NOTA: Questo file è stato rinominato in .txt per evitare errori di compilazione
// in un progetto a modulo singolo. In un vero progetto multi-modulo, si chiamerebbe
// "module-info.java".

module app.service {
    // Questa direttiva rende i tipi `public` del package `com.service`
    // accessibili agli altri moduli che dipendono da `app.service`.
    // Senza questa riga, le classi come `GreetingService` sarebbero
    // considerate private a questo modulo, anche se dichiarate `public`.
    exports com.service;
}
